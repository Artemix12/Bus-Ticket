import {LoginForm} from "@/app/_components/auth-UI/loginForm"

export const metadata={
    title:'Login'
}

export default function Login(){
    return(
        <div>
        <LoginForm/>
        </div>
    )
}